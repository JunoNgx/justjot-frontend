export default function useIconPropsFromTheme() {

    const itemIcontProps = {
        size: 24,
        stroke: 1,
    }

    const themeModeIconProps = {
        size: 18,
        stroke: 1.5,
    }

    return {
        itemIcontProps,
        themeModeIconProps,
    };
};